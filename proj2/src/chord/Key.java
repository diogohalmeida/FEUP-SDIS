package chord;

import java.nio.charset.StandardCharsets;
import java.lang.Math;

public class Key {

    /*
    private final long id;
    private final int KEYSIZE = 32;
    private final int MIN = 0;
    private final double MAX = Math.pow(2, KEYSIZE);

    public Key(String value) throws Exception{
        long auxId = Utils.getHash(value);
        if(auxId < MIN && auxId > MAX) {
            throw new Exception();
        }
        this.id = auxId;
    }

    public long getKeyId() {
        return this.id;
    }
     */
}